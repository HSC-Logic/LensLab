import { expect, test } from '@playwright/test';

test('core simulator journey', async ({ page }) => { await page.goto('/?focal=50&aperture=4&distance=5&sensor=full-frame'); await expect(page.getByText('Frame. Focus.')).toBeVisible(); await page.getByLabel('Focal length slider').fill('85'); await expect(page.getByText('85mm')).toBeVisible(); await page.getByText('Advanced').click(); await expect(page.getByText('Circle of confusion')).toBeVisible(); await page.getByRole('button', { name: 'Compare' }).first().click(); await expect(page.getByText('Setup A versus Setup B')).toBeVisible(); });
for (const viewport of [{ width: 320, height: 568 }, { width: 390, height: 844 }, { width: 768, height: 1024 }, { width: 1440, height: 900 }, { width: 1920, height: 1080 }]) test(`layout ${viewport.width}x${viewport.height}`, async ({ page }) => { await page.setViewportSize(viewport); await page.goto('/'); const overflow = await page.evaluate(() => document.documentElement.scrollWidth > document.documentElement.clientWidth); expect(overflow).toBe(false); });
